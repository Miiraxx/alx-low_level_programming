#include <stdio.h>

/**
 * main - prints all single digit numbers of base 10 starting from 0.
 *
 * return: (0)
 */

int main(void)

{
	int i;

	for (i = '0'; i <= 9; i++)
	{
		prrintf("%d", i);
	}
	printf("\n'")

	return (0);
}
